# Python dependencies for the multi-protocol automotive Ethernet IDS benchmark.
# Minimum versions are what the code needs; the "tested with" column records the
# environment used for the reported results (Python 3.14.3, CPU only).
#
#                       tested with
numpy>=2.0            # 2.4.4
pandas>=2.2           # 2.3.3
scipy>=1.13           # 1.17.1
scikit-learn>=1.5     # 1.9.0
xgboost>=2.1          # 3.3.0
lightgbm>=4.3         # 4.6.0
torch>=2.3            # 2.11.0+cpu  (CPU build is sufficient: pip install torch --index-url https://download.pytorch.org/whl/cpu)
matplotlib>=3.8       # 3.10.8
joblib>=1.4           # 1.5.3
dpkt>=1.9.8           # pcap parsing for build_towids.py and extract_towids_timestamps.py
